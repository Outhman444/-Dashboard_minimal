<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Connexion</title>
    <style>
      :root {
        --blue: #1e7bff;
        --blue-hover: #1069e6;
        --border: #e6e6e6;
        --label: #333;
        --asterisk: #ff4d4f;
        --placeholder: #bfbfbf;
      }
      html, body { height: 100%; }
      body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Oxygen,
          Ubuntu, Cantarell, Fira Sans, Droid Sans, Helvetica Neue, Arial,
          "Noto Sans", sans-serif;
        color: #111;
        background: #fff;
        display: grid;
        place-items: start center;
      }
      .wrap {
        width: 100%;
        max-width: 520px;
        margin-top: min(12vh, 140px);
        padding: 0 22px;
      }
      h1 {
        text-align: center;
        margin: 0 0 28px;
        font-size: 26px;
        font-weight: 700;
      }
      form { width: 100%; }
      .field { margin-bottom: 18px; }
      label {
        display: block;
        margin: 0 0 8px;
        color: var(--label);
        font-size: 14px;
        font-weight: 600;
      }
      .req { color: var(--asterisk); margin-right: 6px; }
      input[type="text"],
      input[type="email"],
      input[type="password"] {
        width: 100%;
        box-sizing: border-box;
        height: 46px;
        padding: 0 14px;
        border: 1px solid var(--border);
        border-radius: 10px;
        outline: none;
        font-size: 14px;
        transition: box-shadow 0.2s, border-color 0.2s;
      }
      input::placeholder { color: var(--placeholder); }
      input:focus {
        border-color: var(--blue);
        box-shadow: 0 0 0 3px rgba(30, 123, 255, 0.15);
      }
      .submit {
        margin-top: 14px;
        width: 100%;
        height: 48px;
        border: none;
        border-radius: 10px;
        background: var(--blue);
        color: #fff;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s, transform 0.02s;
      }
      .submit:active { transform: translateY(1px); }
      .submit:hover { background: var(--blue-hover); }
      .error { color: #e03131; margin-top: 14px; text-align: center; }
    </style>
  </head>
  <body>
    <div class="wrap">
      <h1>Bienvenue</h1>
      <form action="{{ path('app_login') }}" method="post" novalidate>
        <div class="field">
          <label for="username"><span class="req">*</span>Email</label>
          <input
            id="username"
            type="email"
            name="_username"
            value="{{ last_username }}"
            placeholder="Entrer votre email"
            required
            autocomplete="email"
          />
        </div>
        <div class="field">
          <label for="password"><span class="req">*</span>Mot de passe</label>
          <input
            id="password"
            type="password"
            name="_password"
            placeholder="Entrer votre mot de passe"
            required
            autocomplete="current-password"
          />
        </div>
        <button class="submit" type="submit">Enregistrer</button>
      </form>
      {% if error %}
        <div class="error">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
      {% endif %}
    </div>
  </body>
</html>
